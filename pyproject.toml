[project]
name = "color-sampler-desktop"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "PySide6>=6.6",
  "Pillow>=10.0",
  "numpy>=1.24",
  "reportlab>=4.0"
]

[project.scripts]
color-sampler = "app:main"
